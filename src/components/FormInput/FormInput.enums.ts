/* eslint-disable no-shadow */

export enum EFormInputType {
  TEXT = "text",
  TEXTAREA = "textarea",
  SELECT = "select",
  PASSWORD = "password",
  TEL = "tel",
  NUMBER = "number",
  RADIO = "radio",
  CHECKBOX = "checkbox",
  AUTOFILL = "autofill",
  MULTISELECT = "multiselect",
  DATEPICKER = "datepicker",
  FILE = "file",
}

export enum EFormInputStatus {
  ERROR = "error",
  SUCCESS = "success",
}
